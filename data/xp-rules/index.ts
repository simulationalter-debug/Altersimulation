/**
 * Display-layer gamification constants. XP/levels never feed the
 * simulation engine (see engine spec §1.2) — they're purely for the
 * game-feel layer in features/xp and features/streaks.
 */
export const XP_PER_LEVEL = 500;
export const DECISION_XP = 20;
export const JOURNAL_XP = 10;
